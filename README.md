# Bricksplaza

South African e-commerce storefront for bricks, pavers, blocks and related masonry.

The catalogue is generated in the app for now. Accounts, carts, orders and reviews use Postgres (PGLite in local preview, Supabase when `DATABASE_URL` is set). Do not commit secrets or connection strings.
